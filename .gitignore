# ========================================
# Logs & system files 
*.log
.DS_Store
Thumbs.db

# ========================================
# Laravel environment files
.env
.env.*
!.env.example

# ========================================
# Laravel caches & compiled files
.phpunit.result.cache
.phpactor.json
/.phpunit.cache

# ========================================
# IDE & editor folders
/.vscode
/.idea
/.fleet
/.zed

# ========================================
# Laravel Nova (if used)
/.nova

# ========================================
# Composer auth
auth.json

# ========================================
# Node & NPM
/node_modules
/public/hot
/public/build
# ❗ Attention : Railway ne compile pas automatiquement les assets
# Pense à faire : npm install && npm run build && git add public/build -f

# ========================================
# Laravel storage (except public storage symlink)
/storage/*.key
/storage/pail
# Ne PAS ignorer "storage/app/public" car les images sont là
!/storage/app/public
/public/storage
# ⚠️ /public/storage est un symlink => on ne le versionne PAS

# ========================================
# Vendors (composer)
vendor/

# ========================================
# Homestead (if used)
Homestead.yaml
Homestead.json
